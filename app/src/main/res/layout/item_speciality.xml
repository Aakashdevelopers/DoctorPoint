<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_speciality"
    android:layout_width="88dp"
    android:layout_height="114dp"
    android:layout_marginHorizontal="5dp"
    android:layout_marginVertical="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="@color/surface_white"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:strokeColor="@color/card_stroke"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingHorizontal="4dp"
        android:paddingVertical="8dp">

        <FrameLayout
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/bg_squircle_icon">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/iv_speciality_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="Speciality Image"
                android:scaleType="centerCrop"
                android:src="@drawable/banner_1"
                app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.Corner12dp" />

        </FrameLayout>

        <TextView
            android:id="@+id/tv_speciality_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:maxLines="2"
            android:paddingHorizontal="2dp"
            android:text="General Physician"
            android:textColor="@color/text_primary"
            android:textSize="11.5sp"
            android:textStyle="bold" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>